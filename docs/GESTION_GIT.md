# Gestión de DepartamentoT7

Nombre del repositorio y sitio: **DepartamentoT7**. Se mantienen sus identificadores, privacidad y tareas. La carpeta de trabajo conserva su ubicación para mantener válidas las referencias.

Se instaló y leyó la skill git-commit de github/awesome-copilot. Usar Conventional Commits, resumen imperativo de menos de 72 caracteres, un propósito por commit y Refs #N en avances. Cerrar una etapa solo con sus verificaciones registradas. Revisar el diff y seleccionar archivos antes de confirmar; nunca incluir credenciales.

Por petición explícita del usuario se normalizaron los mensajes anteriores. HISTORIAL_COMMITS.json conserva correspondencias y árboles; las ocho revisiones mantienen el mismo contenido, autores y fechas. El historial previo está respaldado en una referencia local y en un bundle verificado fuera del repositorio. La única sustitución de historial publicada usó una condición contra la revisión remota observada; las siguientes subidas son normales, sin reescritura.

Las huellas históricas en los informes de T04–T12 corresponden a los hechos comprobados entonces y se interpretan mediante esa tabla. No se alteran retroactivamente las evidencias. El nombre anterior de los informes históricos queda registrado como procedencia.

Trabajar por etapas completas; realizar las tareas en orden de dependencia, agrupar comprobaciones independientes y repetir solo las afectadas. No marcar validaciones pendientes como aprobadas.
